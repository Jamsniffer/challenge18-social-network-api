const { User } = require('../models');

const userController = {
    //get all users

    getAllUsers(req, res) {
        
    }
    
    //get one user by id

    //create user

    //delete a user

    //update user
}

module.export = userController